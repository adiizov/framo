import Navbar from "@/blocks/navbar/Navbar";

const Header = () => {
    return (
        <div className="bg-foreground">
            <Navbar />
            header
        </div>
    );
};

export default Header;